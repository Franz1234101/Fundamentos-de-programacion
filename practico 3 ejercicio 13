#include <stdio.h>

int main() {
    int i, j;
    printf("Tabla de sumar (1 al 10):\n\n");
    for(i = 1; i <= 10; i++) {
        for(j = 1; j <= 10; j++) {
            printf("%2d + %2d = %2d\t", i, j, i + j);
        }
        printf("\n");
    }
    return 0;
}
