#include <stdio.h>

int main() {
    int N, i;
    long producto = 1;
    printf("Ingrese N: ");
    scanf("%d", &N);

    for(i = 1; i <= N; i++) {
        producto *= i;
    }

    printf("El producto es: %ld\n", producto);
    return 0;
}
