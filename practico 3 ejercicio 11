#include <stdio.h>

int main() {
    int a, b, resto;
    printf("Ingrese dos numeros: ");
    scanf("%d %d", &a, &b);

    while(b != 0) {
        resto = a % b;
        a = b;
        b = resto;
    }

    printf("El MCD es: %d\n", a);
    return 0;
}
